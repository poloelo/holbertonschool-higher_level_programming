The ``MyList`` module
======================

Using ``MyList``
----------------

Import the class:
    >>> MyList = __import__('1-my_list').MyList

Test basic functionality:
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> print(my_list)
    [1, 4, 2, 3, 5]
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]
    >>> print(my_list)
    [1, 4, 2, 3, 5]

Test with negative numbers:
    >>> my_list2 = MyList()
    >>> my_list2.append(3)
    >>> my_list2.append(-1)
    >>> my_list2.append(4)
    >>> my_list2.append(0)
    >>> my_list2.print_sorted()
    [-1, 0, 3, 4]

Test with duplicates:
    >>> my_list3 = MyList([5, 5, 2, 2, 1])
    >>> my_list3.print_sorted()
    [1, 2, 2, 5, 5]

Test with empty list:
    >>> my_list4 = MyList()
    >>> my_list4.print_sorted()
    []

Test with one element:
    >>> my_list5 = MyList([1])
    >>> my_list5.print_sorted()
    [1]

Test with already sorted list:
    >>> my_list6 = MyList([1, 2, 3, 4])
    >>> my_list6.print_sorted()
    [1, 2, 3, 4]

Test with reverse sorted list:
    >>> my_list7 = MyList([5, 4, 3, 2, 1])
    >>> my_list7.print_sorted()
    [1, 2, 3, 4, 5]

Test that it's a subclass of list:
    >>> issubclass(MyList, list)
    True

Test instance type:
    >>> my_list = MyList()
    >>> type(my_list) == MyList
    True
    >>> isinstance(my_list, list)
    True
